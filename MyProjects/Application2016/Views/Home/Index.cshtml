@model Application2016.Models.ProductConditions
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}


@section MainContent{
     <!-- Slider -->
     @*@Html.Action("BoxSlideShow", "Base")*@
      <!-- end Slider --> 
    @* @Html.Action("BoxService", "Base")*@

    <!-- Danh sách nhà -->
    <section class="main-container col1-layout home-content-container">
        <div class="container">
            <div class="row">
                <div class="std">
                    <div id="productList">
                    @Html.Action("BoxProduct","Base", Model)
                    </div>
                    @*@Html.Action("BoxProductFeature", "Base")*@
                </div>
            </div>
        </div>
    </section>
}